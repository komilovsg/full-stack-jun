# Анализ проблем с API (DeepSeek и Gemini)

## 🔍 Обнаруженные проблемы

### 1. DeepSeek API - Ошибка 402 (Insufficient Balance)

**Проблема:**
```
❌ DeepSeek API ошибка: 402 Payment Required
   Детали: {"error":{"message":"Insufficient Balance","type":"unknown_error","param":null,"code":"invalid_request_error"}}
```

**Причина:**
- На аккаунте DeepSeek недостаточно средств на балансе
- DeepSeek не предоставляет бесплатный тариф - требуется оплата

**Решение:**
- Пополнить баланс на аккаунте DeepSeek (https://platform.deepseek.com/)
- Или использовать только Gemini API (который имеет бесплатный тариф)

**Текущая логика:**
- Код правильно обрабатывает ошибку 402 и переключается на Gemini как fallback
- Пользователь получает понятное сообщение об ошибке

---

### 2. Gemini API - Ошибка 404 (Model Not Found)

**Проблема:**
```
❌ Gemini API ошибка: [404 Not Found] models/gemini-1.5-flash is not found for API version v1beta
```

**Причина:**
- Модель `gemini-1.5-flash` недоступна в API версии v1beta
- Библиотека `@google/generative-ai` использует v1beta по умолчанию
- Старые модели (`gemini-pro`) могут быть недоступны в новых версиях API

**Решение:**
- Изменена основная модель на `gemini-1.5-flash` (должна работать в v1 API)
- Добавлен fallback на `gemini-1.5-pro` и `gemini-pro`
- Улучшена обработка ошибок с детальным логированием

**Доступные модели в бесплатном тарифе Gemini (январь 2026):**
- `gemini-3-flash-preview` - 15 RPM, 1500 RPD, 2M токенов (рекомендуется)
- `gemini-2.5-flash` - 15 RPM, 1500 RPD, 1M токенов
- `gemini-2.5-pro` - 2 RPM, 50 RPD, 2M токенов
- `gemini-1.5-flash` - устаревшая модель (fallback)
- `gemini-1.5-pro` - устаревшая модель (fallback)
- `gemini-pro` - старая модель (fallback)

---

## 🔧 Внесенные изменения

### 1. `bot/src/services/geminiService.ts`
- Обновлены модели на актуальные для января 2026: `gemini-3-flash-preview`, `gemini-2.5-flash`, `gemini-2.5-pro`
- Добавлены Safety Settings (`BLOCK_ONLY_HIGH`) для избежания блокировок при анализе сообщений
- Реализован retry механизм с экспоненциальной задержкой для обработки Rate Limiting (429)
- Улучшен fallback механизм для переключения между моделями
- Добавлено детальное логирование ошибок

### 2. `bot/src/services/deepseekService.ts`
- Уже правильно обрабатывает ошибку 402 (Insufficient Balance)
- Бросает специальную ошибку `DEEPSEEK_INSUFFICIENT_BALANCE` для корректного fallback

### 3. `bot/src/handlers/analyzeHandlers.ts`
- Правильно обрабатывает ошибки от обоих API
- Переключается с DeepSeek на Gemini при ошибке 402
- Показывает понятные сообщения пользователю

---

## 📝 Рекомендации

### Для DeepSeek:
1. **Пополнить баланс** на https://platform.deepseek.com/
2. Или **отключить DeepSeek** и использовать только Gemini (бесплатный тариф)

### Для Gemini:
1. **Проверить API ключ** - убедиться, что он активен
2. **Проверить лимиты** - бесплатный тариф имеет ограничения (январь 2026):
   - `gemini-3-flash-preview`: 15 RPM, 1500 RPD, 2M токенов
   - `gemini-2.5-flash`: 15 RPM, 1500 RPD, 1M токенов
   - `gemini-2.5-pro`: 2 RPM, 50 RPD, 2M токенов
3. **Использовать Flash модели** - они имеют более мягкие лимиты (15 RPM vs 2 RPM)
4. **Важно:** На бесплатном тарифе данные используются для обучения Google. Для приватных данных рассмотрите платный тариф (~$0.10 за 1M токенов)

### Общие рекомендации:
- ✅ Retry с экспоненциальной задержкой уже реализован для обработки Rate Limit
- Кэшировать результаты анализа (уже реализовано для статистики)
- Мониторить логи для отслеживания ошибок API
- Для приватных/коммерческих данных рассмотреть переход на платный тариф Gemini

---

## 🧪 Тестирование

После внесенных изменений:

1. **Пересобрать Docker образ:**
   ```bash
   docker-compose build bot
   docker-compose up -d bot
   ```

2. **Проверить логи:**
   ```bash
   docker-compose logs bot -f
   ```

3. **Протестировать команду `/analyze`:**
   - Отправить команду в групповом чате
   - Проверить, что используется правильная модель Gemini
   - Убедиться, что fallback работает корректно

---

## 📊 Статус

- ✅ **Gemini API**: Обновлено - используются актуальные модели (`gemini-3-flash-preview`, `gemini-2.5-flash`) с fallback
- ✅ **Safety Settings**: Настроены для избежания блокировок (`BLOCK_ONLY_HIGH`)
- ✅ **Rate Limiting**: Реализован retry с экспоненциальной задержкой
- ⚠️ **DeepSeek API**: Требуется пополнение баланса или отключение
- ✅ **Обработка ошибок**: Улучшена с детальным логированием
- ✅ **Fallback механизм**: Работает корректно

---

## 🔒 Конфиденциальность данных

**Важно:** На бесплатном тарифе Gemini API Google имеет право использовать ваши входные данные (сообщения из групп) и ответы модели для обучения и улучшения своих алгоритмов.

**Рекомендации:**
- Для приватных или коммерческих данных рассмотрите переход на платный тариф (~$0.10 за 1M токенов для Gemini 3 Flash)
- На платном тарифе данные не используются для обучения и остаются конфиденциальными
